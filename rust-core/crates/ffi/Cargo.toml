[package]
name = "ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
domain = { path = "../domain" }
infrastructure = { path = "../infrastructure" }
serde_json = "1"
tracing = "0.1"
